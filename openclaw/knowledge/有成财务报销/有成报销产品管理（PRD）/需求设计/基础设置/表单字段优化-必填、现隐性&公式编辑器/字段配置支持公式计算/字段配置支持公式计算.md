---
title: "字段配置支持公式计算"
nodeId: vy20BglGWOeObxmXHlErOD3gJA7depqY
workspaceId: R2PmK2gngjVnZXvp
docUrl: "https://alidocs.dingtalk.com/i/nodes/vy20BglGWOeObxmXHlErOD3gJA7depqY?utm_scene=team_space"
updateTime: 1751427317000
exportedAt: 2026-08-05T04:26:20.609Z
source: dingtalk-document-mcp
---
# 字段配置支持公式计算

| **修订时间** | **版本** | **修订说明** | **作者** |
|----------------|----------|----------------|----------|
| 20231226 | v1.0 | 新建 | 昭觉 |

## 1、涉及系统

有成报销 PC  &  MB

## 2、需求背景

客户：浙江省财开

需求背景：目前系统的计算公式仅支持数字、金额组件进行计算，客户在填写单据时会有较多的重复字段，客户希望这些字段间能实现自动带入，不需要员工重复填写

## 3、业务流程/架构

如复杂需求，放置相关流程/架构图

## 4、功能清单

根据个人需要补充

| **模块** | **功能点** | **备注** |
|----------|-------------|----------|
| 自定义单据&自定义费用 | 模板中字段取值规则支持计算公式 |  |
| 新建&编辑单据 | PC&MB |  |
| 自动生成费用 |  |  |
| 历史数据 |  |  |

## 5、需求详情

### 5.1、需求说明

#### 自定义单据&自定义费用：字段增加取值规则



![Picture 1](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/ZWGl05mdPA6JDn34/img/508de95d-655d-40ee-8070-0b45dd493793.png?Expires=1785910959&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=mk978hN4ys1N9RVzFkm1xiBWdl8%3D "")

字段增加取值规则（存在取值规则的字段，增加【按照计算公式（不可编辑）、按照计算公式（可编辑）】）：

按照计算公式（不可编辑）：则该字段根据公式配置取值，在新建或者编辑单据时不可编辑该字段

按照计算公式（可编辑）：该字段根据公式配置取值，但是在新建或者编辑单据时允许编辑

#### 计算公式优化



![Picture 2](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/ZWGl05mdPA6JDn34/img/5230b4da-3014-4d2a-86b6-12ccdce1b86c.png?Expires=1785910959&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=Wl3mWBhIm%2Bsk%2FmnZ2QokWkUY8v8%3D "")

##### 字段变量

字段变量展示 当前模板的字段以及模板中配置的消费明细、行程组件、产品明细组件、表格组件等组件中的字段

参与计算的字段包括：文本组件、数字组件、金额组件、日期组件、部门组件、员工组件、城市组件、辅助核算、月度范围、角色组件、链接、手机号

##### 函数

支持集合函数、日期函数、逻辑函数、数学函数、字符串函数

###### 集合函数

**IN**

<span style="color: #171A1D;">IN(ele,collection) 用于判断元素是否位于集合中。</span>

<span style="color: #171A1D;">示例：IN('选项 1', LIST('选项 1','选项 2')) 的结果为true。</span>

**INTER**

<span style="color: #171A1D;">INTER(collection1,collection2) 用于计算两个集合的交集。</span>

<span style="color: #171A1D;">示例：INTER(LIST(1,2,3),LIST(2,3,5,6)) 的结果是 LIST(2,3)。</span>

**SUPPLE**

<span style="color: #171A1D;">SUPPLE(collection1,collection2) 用于计算两个集合的补集。</span>

<span style="color: #171A1D;">示例：SUPPLE(LIST(1,2,3,4),LIST(3,4)) 的结果是 LIST(1,2)。</span>

**LIST**

<span style="color: #171A1D;">LIST(ele1, ele2, ...) 用于生成一个由 ele1、ele2 ... 组成的集合。</span>

<span style="color: #171A1D;">示例：LIST('选项1','选项2')。</span>

**UNION**

<span style="color: #171A1D;">UNION(collection1,collection2) 用于计算两个集合的并集。</span>

<span style="color: #171A1D;">示例：UNION(LIST(1,2),LIST(3,4)) 的结果是 LIST(1,2,3,4)。</span>

###### 日期函数

**DATEDELTA**

<span style="color: #171A1D;">DATEDELTA(date, deltadays) 用于将指定日期加或减指定天数，正数为增加，负数为减少。</span>

<span style="color: #171A1D;">示例1：DATEDELTA(date, 1) date的日期加一天。</span>

<span style="color: #171A1D;">示例2：DATEDELTA(date, - 1) date日期减一天。</span>

**DATEDIF**

<span style="color: #171A1D;">DATEDIF(startDate, endDate, \[unit\]) 用于计算两个时间的差值。startDate和endDate为必填参数，Unit可选。</span>

:::info<span style="color: #171A1D;">Unit可取值：</span>
- <span style="color: #171A1D;">"y"：表示年数。</span>
- <span style="color: #171A1D;">"M"：表示月数。</span>
- <span style="color: #171A1D;">"d"：表示天数，默认值。</span>
- <span style="color: #171A1D;">"h"：表示小时数。</span>
- <span style="color: #171A1D;">"m"：表示分钟数。</span>
- <span style="color: #171A1D;">"s"：表示秒数</span>

:::

<span style="color: #171A1D;">示例：DATEDIF('2020-01-01','2020-01-02','d') 结果是1天。</span>

**DATE**

<span style="color: #171A1D;">DATE(string) 用于获得给定字符串代表的日期。</span>

:::info<span style="color: #171A1D;">DATE(string)函数内的参数字符串支持以下格式：</span>
- <span style="color: #171A1D;">yyyy-MM-dd HH:mm:ss</span>
- <span style="color: #171A1D;">yyyy-MM-dd HH:mm</span>
- <span style="color: #171A1D;">yyyy-MM-dd HH</span>
- <span style="color: #171A1D;">yyyy-MM-dd</span>

:::

<span style="color: #171A1D;">示例：DATE('2020-12-28 11:11:00') 返回该字符串代表的日期。</span>

<span style="color: #171A1D;">DATE(number) 用于获得给定时间戳（毫秒单位）代表的日期。</span>

<span style="color: #171A1D;">示例：DATE(1647592883824) 返回该时间戳（毫秒单位）代表的日期。</span>

**YEAR**

<span style="color: #171A1D;">YEAR(date) 用于获取某日期内的年份信息。</span>

<span style="color: #171A1D;">示例：YEAR('2020-12-09') 的结果是2020。</span>

**MONTH**

<span style="color: #171A1D;">MONTH(date)用于获取某日期内的月份信息，月份是介于1到12之间的整数。</span>

<span style="color: #171A1D;">示例：MONTH('2020-12-09 11:03:04') 的结果是12。</span>

**DAY**

<span style="color: #171A1D;">DAY(date) 用于获取某个日期内的天数信息，获取的天数是介于1到31之间的整数。</span>

<span style="color: #171A1D;">示例：DAY('2020-01-02') 的结果是2。</span>

**HOUR**

<span style="color: #171A1D;">HOUR(date) 用于获取某日期内的小时数信息。</span>

<span style="color: #171A1D;">示例：HOUR('2020-12-09 11:03:04') 的结果是11。</span>

**MINUTE**

<span style="color: #171A1D;">MINUTE(date) 用于获取某日期内的分钟数信息。</span>

<span style="color: #171A1D;">示例：MINUTE('2020-12-09 12:03:04') 的结果是3。</span>

**SECOND**

<span style="color: #171A1D;">SECOND(date) 用于返回某日期内的秒数信息。</span>

<span style="color: #171A1D;">示例：SECOND('2021-01-12 12:23:32') 返回32。</span>

**NETWORKDAYS**

<span style="color: #171A1D;">NETWORKDAYS(startDate,endDate,\[holidays\]) 用于获取参数start\_date和end\_date之间完整的工作日数值，工作日不包括周末和参数holidays指定的假期。</span>

<span style="color: #171A1D;">示例：NETWORKDAYS('2020-12-26', '2021-01-01'， '2020-01-01') 的结果是4。</span>

**NOW**

<span style="color: #171A1D;">NOW() 用于返回当前时间。</span>

<span style="color: #171A1D;">示例：NOW()。</span>

**TIMESTAMP**

<span style="color: #171A1D;">TIMESTAMP(date) 用于将日期对象转换成时间戳，单位毫秒。</span>

<span style="color: #171A1D;">示例：TIMESTAMP('2020-12-23 12:23:34') 返回1608697414000。</span>

**DATEFORMAT**

<span style="color: #171A1D;">DATEFORMAT(date,format) 用于将日期格式化为指定类型。</span>

<span style="color: #171A1D;">示例：DATEFORMAT(NOW(),'yyyy/MM/dd HH:mm:ss') 的结果是2020/01/02 17:02:30（依据当前时间）。</span>

###### 逻辑函数

**AND**

<span style="color: #171A1D;">AND(logic1, logic2..) 表示只要有一个参数是false就返回false，所有参数都为true才返回true；集合参数会被自动展开成多个参数，遇到</span><span style="color: #171A1D;">false</span><span style="color: #171A1D;">会自动短路。</span>

<span style="color: #171A1D;">示例：AND(true,false,true) 的结果是 false；AND(LIST(true,false,true)) 的结果也是false。</span>

**EQ**

<span style="color: #171A1D;">EQ(value1, value2)表示如果value1和value2的值相等，则为true，否则为false。</span>

<span style="color: #171A1D;">示例：EQ('aa', 'aa')和EQ(1,1) 的结果为true。</span>

**TRUE**

<span style="color: #171A1D;">TRUE() 返回true。</span>

<span style="color: #171A1D;">示例：TRUE()。</span>

**FALSE**

<span style="color: #171A1D;">FALSE() 返回false。</span>

<span style="color: #171A1D;">示例：FALSE()。</span>

GE

<span style="color: #171A1D;">GE(value1, value2) 表示如果value1大于等于value2返回true，反之则返回false。</span>

<span style="color: #171A1D;">示例：GE(2, 1)和GE(1, 1)的结果都是true。</span>

**GT**

<span style="color: #171A1D;">GT(value1,value2) 表示如果value1大于value2则返回true，反之返回false。</span>

<span style="color: #171A1D;">示例：GT(2,1)的结果为true。</span>

**IF**

<span style="color: #171A1D;">IF(logic, value1, value2) 表示如果logic为true，则返回value1， 否则返回value2。</span>

<span style="color: #171A1D;">示例：IF(70\>=60, '及格', '不及格') 的结果是及格。</span>

**ISEMPTY**

<span style="color: #171A1D;">ISEMPTY(param) 表示参数如果为空，则返回为true，反之则返回false。</span>

<span style="color: #171A1D;">示例：ISEMPTY(' ') 的结果为true。</span>

LE

<span style="color: #171A1D;">LE(value1, value2) 表示如果value1小于等于value2则返回true，反之返回false。</span>

<span style="color: #171A1D;">示例：LE(1,2) 和LE(1,1)的结果都为true。</span>

**LT**

<span style="color: #171A1D;">LT(value1,value2) 表示如果value1小于value2则返回true，反之返回false。</span>

<span style="color: #171A1D;">示例：LT(1,2) 的结果为true。</span>

**NE**

<span style="color: #171A1D;">NE(value1, value2) 表示如果value1和value2的值不相等，则返回为true，否则返回false。</span>

<span style="color: #171A1D;">示例：NE(1,2) 的结果为false。</span>

**NOT**

<span style="color: #171A1D;">NOT(logic) 表示获取参数的取反，如果参数是true,则返回false ，如果参数是false，则返回true。</span>

<span style="color: #171A1D;">示例：NOT(true) 的结果为false。</span>

**OR**

<span style="color: #171A1D;">OR(logic1, logic2..) 表示只要有一个参数是true就返回true，所有参数都为false才返回false；集合参数会被自动展开成多个参数，遇到</span><span style="color: #171A1D;">true</span><span style="color: #171A1D;">会自动短路。</span>

<span style="color: #171A1D;">示例：OR(true,false) 的结果是true；OR(LIST(true,false)) 的结果是true。</span>

**XOR**

<span style="color: #171A1D;">XOR(logic1, logic2) 表示异或运算，如果两个参数不一样则返回true，如果两个参数一样则返回false。</span>

<span style="color: #171A1D;">示例：XOR(true, true) 的结果为false。</span>

###### 数学函数

**ABS**

<span style="color: #171A1D;">ABS(number) 返回数字的绝对值。</span>

<span style="color: #171A1D;">示例：ABS(-123.456) 的结果为123.456。</span>

**AVERAGE**

<span style="color: #171A1D;">AVERAGE(number1, number2, ...) 求数字的平均值。</span>

<span style="color: #171A1D;">示例：AVERAGE(1,2) 的结果为1.5。</span>

**CEILING**

<span style="color: #171A1D;">CEILING(number, significance) 返回将参数number向上舍入（沿绝对值增大的方向）为最接近的指定基数的倍数。</span>

<span style="color: #171A1D;">示例：CEILING(22.43, 2) 向上取整到2的倍数，所以返回24。</span>

**FIXED**

<span style="color: #171A1D;">FIXED(number) 将数字向下舍入到指定的小数位数。</span>

<span style="color: #171A1D;">示例：FIXED(10.8963, 2) 返回的结果是10.89。</span>

**INT**

<span style="color: #171A1D;">INT(number)将数字向下舍入到最接近的整数。</span>

<span style="color: #171A1D;">示例：INT(3.45) 返回3；INT(-3.45) 返回-4。</span>

**MAX**

<span style="color: #171A1D;">MAX(number1, number2, ...) 获取这组数字中的最大值。</span>

<span style="color: #171A1D;">示例：MAX(1, 4, 6.7, 10, 2) 返回10。</span>

**MIN**

<span style="color: #171A1D;">MIN(number1, number2, ...) 返回数组中的最小值。</span>

<span style="color: #171A1D;">示例：MIN(1, 3, 5, 7, 2, 4) 返回1。</span>

**MOD**

<span style="color: #171A1D;">MOD(number, divisor) 返回两数相除的余数。</span>

<span style="color: #171A1D;">示例：MOD(37, 6) 返回值为1。</span>

**PI**

<span style="color: #171A1D;">PI() 返回圆周率3.14159265358979323846。</span>

<span style="color: #171A1D;">示例：计算半径长为r的圆的面积 PI() \* POWER(r, 2) 如果r=1，那么返回3.14159265358979323846。</span>

**POWER**

<span style="color: #171A1D;">POWER(number, power) 返回数字乘幂的结果。</span>

<span style="color: #171A1D;">示例：POWER(2, 2) 的结果是4。</span>

**PRODUCT**

<span style="color: #171A1D;">PRODUCT(number1, number2...) 函数将所有参数相乘并返回乘积。</span>

<span style="color: #171A1D;">示例：PRODUCT(2, 3) 的结果是6。</span>

**RAND**

<span style="color: #171A1D;">RAND() 返回大于等于 0 且小于 1 的均匀分布随机实数。每一次触发计算都会变化。</span>

<span style="color: #171A1D;">示例：RAND() 的结果是0.601931207820683。</span>

**ROUND**

<span style="color: #171A1D;">ROUND(number, numDigits) 将数字四舍五入到指定的位数。</span>

<span style="color: #171A1D;">示例：ROUND(1.2345, 2) 返回1.23；ROUND(12345, 2) 返回12345。</span>

**SUM**

<span style="color: #171A1D;">SUM(number1, number2...) 函数将所有参数求和并返回，集合参数会被自动展开成多个参数。</span>

<span style="color: #171A1D;">示例：SUM(1.23, 1.45, 100) 返回102.68；SUM(LIST(1,2,3,4,5)) 的结果是15。</span>

###### 字符串函数

**CONCATENATE**

<span style="color: #171A1D;">CONCATENATE(text1, text2, text3...) 用于将多个字符串类型的参数拼接后返回新的字符串。</span>

<span style="color: #171A1D;">示例：CONCATENATE('A', 'B') 的结果为'AB'。</span>

**LOWER**

<span style="color: #171A1D;">LOWER(text) 用于将参数中的所有字母转换成小写字母并返回。</span>

<span style="color: #171A1D;">示例：LOWER('AbCd') 的结果是'abcd'。</span>

**REPLACE**

<span style="color: #171A1D;">REPLACE(oldText, startNum, numChars, newText) 用于将oldText的从startNum开始的numChars个字符替换成newText，startNum从1开始。</span>

<span style="color: #171A1D;">示例：REPLACE('12345678', 2, 3, 'ABCD') 结果是1ABCD5678。</span>

**UPPER**

<span style="color: #171A1D;">UPPER(text) 用于将文本字符串中的所有小写字母转换成大写字母。</span>

<span style="color: #171A1D;">示例：UPPER('AbCd') 返回结果是'ABCD'。</span>

##### 运算符

###### 逻辑运算符

~~包括：&&、 \|\|、！~~

~~&&：与运算符，只有两个值都为TRUE时结果才是TRUE~~

~~\|\|：或运算符，只要有一个值为TRUE，结果就为TRUE~~

~~！：非运算符，对值取反；值为TRUE，则结果为FALSE~~

###### 算数运算符

~~包括：加、减、乘、除、求余（%）~~

###### 关系运算符

~~包括：\>、\<、\>=、\<=、==、!=~~

##### 计算公式

1、中间区域展示计算公式的配置，取消当前版本的数字运算符、括号、数字键盘展示，支持键盘直接输入，不限制输入的内容，但是在语法校验或者点击【确定】时，针对特殊字符提示：公式语法错误，不支持特殊字符计算

2、清空：点击，则清空输入的内容

3、语法检查：点击，则根据输入框中输入的内容进行语法校验，如果语法校验不通过，则给出提示
- 如果是中英文输入不合法，则提示：计算公式中XXXX中英文输入不合法
- 如果有特殊字符，则提示：计算公式语法错误，不支持特殊字符计算
- 如果是字段类型不匹配，比如数字组件配置的计算因子为文本或者部门，提示：公式的返回值类型不匹配
- 使用函数时，点击确定或者语法检查，如果参数类型不合法，提示：函数\{函数名称\}的第\{N\}个参数类型错误

:::info注：在不使用右侧函数时：

1、文本组件支持文本、员工、数字、金额、部门、城市、辅助核算、日期、月度范围、角色组件、链接组件、手机号参与计算

2、数字组件和金额组件均支持数字、金额组件参与计算

3、员工组件支持员工组件参与计算；如果计算因子是多选，公式值是单选，那么只会带入计算因子的第一个值

4、部门组件支持部门组件参与计算

5、城市组件仅对应城市组件参与计算

6、辅助核算组件也仅支持辅助核算组件参与计算；如果计算因子和公式值的类别不同，则提单或者编辑时无法带入

7、月度范围：仅对应月度范围

8、角色组件：仅支持角色组件

9、链接：不支持参与计算

10、手机号：仅支持手机号

:::

##### 说明

当选择右侧的函数时，说明部分展示公式的说明及示例。

:::info例如：

<span style="color: #171A1D;">AND(logic1, logic2..) 表示只要有一个参数是false就返回false，所有参数都为true才返回true；集合参数会被自动展开成多个参数，遇到</span><span style="color: #171A1D;">false</span><span style="color: #171A1D;">会自动短路。</span>

<span style="color: #171A1D;">示例：AND(true,false,true) 的结果是 false；AND(LIST(true,false,true)) 的结果也是false。</span>

:::

#### 自动生成费用

1、里程补贴和第三方订单的生成：

不受公式配置的影响，保持当前的业务逻辑不变，业务校验也保持不变

比如里程补贴的校验：修改后的金额超过里程数，则不允许修改等逻辑；

**2、发票生成消费：发票上传生成消费、OCR识票生成消费时，在“取值规则”为“按公式计算”时，依然按照当前的逻辑：**
- 按公式计算不可编辑时：发票金额不会带入配置公式的金额字段，且提单或者新建单据时不可编辑
- 按公式计算可编辑时：如果字段没有值，则带入发票金额，如果有值则不自动填充；提单或者编辑时允许编辑

#### 历史数据

1、历史单据和费用中已配置的字段默认刷上【按公式计算（不可编辑）、按公式计算（可编辑）】的取值规则

2、历史配置的计算公式仍然有效，点击编辑时，仍然展示历史配置的公式，编辑后按照新的公式进行计算


