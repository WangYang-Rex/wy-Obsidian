---
title: "【成都俊云科技】【有成报销v2.0】仅高德打车邮件发票附件保存优先级优化-精简版"
nodeId: Obva6QBXJw9lbx7RT2mbO0oyWn4qY5Pr
workspaceId: R2PmK2gngjVnZXvp
docUrl: "https://alidocs.dingtalk.com/i/nodes/Obva6QBXJw9lbx7RT2mbO0oyWn4qY5Pr?utm_scene=team_space"
exportedAt: 2026-05-08T08:07:06.110Z
source: dingtalk-document-mcp
---
```
| 修订时间 | 版本 | 修订说明 | 负责人 |
| --- | --- | --- | --- |
| 20260414 | v1.0 | 初始版本：邮件获取发票合并消费时，按pdf>ofd>xml优先保存，仅存储一份 | 产品部 |
```

---

## **一、研发速览**

| 项 | 内容 |
|----------------------------------------------------------------------|-------------------------------------------------------------------------|
| 需求摘要 | 邮件获取发票合并生成消费时，同一发票多格式附件按pdf\>ofd\>xml优先级保存 |
| 涉及页面 | 我的-待办-消费详情页 |
| 数据库变更 | 否，附件存储表结构不变，变更附件选取保存逻辑 |
| 存量兼容 | 是，存量已生成消费保留原有附件数据，仅新拉取的邮件发票走新逻辑 |
| 关联系统 | 无 |
> *关键边界：仅影响"同一封邮件内的发票和行程单合并为一笔消费"开启时的保存规则；不开启合并时，三个格式仍分别生成三个消费，不受影响。*   

---

## **二、需求背景**

### **2.1 现状**
- 功能位置：邮箱绑定-发票自动生成消费（全局设置-费用设置-同一封邮件内的发票和行程单合并为一笔消费）
- 当前逻辑：系统从邮件中获取发票附件时，同一张发票可能包含pdf、ofd、xml三种格式文件。开启"同一封邮件内的发票和行程单合并为一笔消费"后，这三个格式文件的发票仅会合并到一个消费中，且仅保存一个格式的发票文件。消费详情里当前展示的是xml格式附件。
- 问题：xml格式不便于用户直接查看发票票面信息，客户期望按pdf\>ofd\>xml优先级保存发票附件。

### **2.2 目标**

在"同一封邮件内的发票和行程单合并为一笔消费"开启时，系统从邮件获取同一发票的多个格式附件后，按 pdf \> ofd \> xml 优先级选取并保存一份附件到消费中，未开启合并时保持现有逻辑不变。

---

## **三、功能变更清单**

| 变更类型 | 功能点 | 一句话说明 | 优先级 |
|-------------------------------------------------------------------------------|----------------------------------------------------------------------------|----------------------------------------------------------------------------------|----------------------------------------------------------------------------|
| 修改 | 仅高德打车邮件发票附件保存优先级 | 合并消费模式下，同一发票多格式附件按pdf\>ofd\>xml优先级保存一份 | P0 |

---

## **四、功能描述**

### **4.1 邮件发票附件保存优先级优化**

位置 邮件拉取发票服务 → 附件解析与保存逻辑

变更内容
- 系统从高德打车邮件中解析同一发票的多个格式附件（pdf、ofd、xml）时，按 pdf \> ofd \> xml 优先级选取一份附件保存到消费中
- 未开启"同一封邮件内的发票和行程单合并为一笔消费"时，保持现有逻辑（三种格式分别生成三个消费，每个消费存储对应格式附件）

处理逻辑
1. 系统从邮件中拉取发票附件，识别同一发票关联的多个格式文件（pdf、ofd、xml）
2. 判断"发票和行程单合并为一笔消费"开关状态：
    - 若开关开启，则按 pdf \> ofd \> xml 优先级，将优先级最高的一份附件关联到生成的消费中
    - 若开关关闭，则保持现有逻辑，各格式附件分别生成独立消费并各自存储
3. 若同一发票仅存在一种格式附件，则直接保存该格式

边界处理

| 场景 | 处理方案 |
|-------------------------------------------------------------------------|-------------------------------------------------------------------------------|
| 同一发票仅有xml格式 | 保存xml格式 |
| 同一发票有pdf和xml，无ofd | 保存pdf格式 |
| 同一发票有ofd和xml，无pdf | 保存ofd格式 |
| 同一发票同时有pdf、ofd、xml | 保存pdf格式 |
| 存量消费 | 历史已生成的消费保留原有附件数据，不触发清理 |
| 非邮件拉取的消费 | 手动上传、OCR识别等其他来源生成的消费，保存逻辑不受影响 |

---

### **4.2 消费详情附件展示**

位置 我的-待办-消费详情页

![image.png](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/1X3lE5jA1Va6BlJb/img/d1f60b26-3387-4bdc-a270-309af8f58740.png?Expires=1778231617&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=iutJkEfJJYxiChOS%2FlSI6pyooAM%3D "")

变更内容
- 消费详情页直接展示该消费下已保存的发票附件

处理逻辑
1. 消费详情页加载时，读取该消费关联的发票附件
2. 直接展示附件

边界处理

| 场景 | 处理方案 |
|-------------------------------------------------------------------------|-------------------------------------------------------------------------------|
| 存量消费 | 历史已生成的邮件消费，详情页仍展示已保存的附件（如存量为xml则继续展示xml） |

---

## **五、验收标准**

| 序号 | 验收项 | 验收标准 |
|-------------------------------------------------------------------------|----------------------------------------------------------------------------|-------------------------------------------------------------------------------|
| 1 | 保存优先级-pdf优先 | 同一发票包含pdf、ofd、xml三种格式时，消费中保存pdf格式附件 |
| 2 | 保存优先级-ofd优先 | 同一发票仅包含ofd和xml格式时，消费中保存ofd格式附件 |
| 3 | 保存优先级-xml兜底 | 同一发票仅包含xml格式时，消费中保存xml格式附件 |
| 4 | 合并模式限制 | 未开启"同一封邮件内的发票和行程单合并为一笔消费"时，三种格式仍分别生成三个消费，各存对应格式附件 |
| 5 | 存量消费兼容 | 历史已生成的邮件消费，附件数据不被清理，保持原有存储状态 |
| 6 | 消费详情展示 | 新拉取生成的消费，详情页展示按优先级保存的那份附件 |
| 7 | 非邮件消费不受影响 | 手动上传、OCR识别等其他来源生成的消费，保存和展示逻辑均不受影响 |

**六、完整prd**

[20260414高德打车邮件发票附件保存优先级优化【有成报销v2.0】【成都俊云科技】](https://alidocs.dingtalk.com/i/nodes/QOG9lyrgJP3OYPnGTzmDYk46VzN67Mw4?iframeQuery=utm_source=portal&utm_medium=portal_space_create)


