---
title: "按批次_保质期扣减成本"
nodeId: qnYMoO1rWxDlYrR6CKM5g604W47Z3je9
workspaceId: R2PmK2Q8rxRbeXvp
docUrl: "https://alidocs.dingtalk.com/i/nodes/qnYMoO1rWxDlYrR6CKM5g604W47Z3je9?utm_scene=team_space"
exportedAt: 2026-03-31T04:10:49.154Z
source: dingtalk-document-mcp
---
# 按批次/保质期扣减成本

| **修订时间** | **版本** | **修订人** | **修订说明** |
|----------------|----------|-------------|----------------|
| 20220718 | v1.0 | 黄艺平 | 新建 |
| 20220802 | v2.0 | 黄艺平 | 修改：1、期初存货核算方式变更时，处理逻辑变更；2、出入库记录：增加功能上线时，刷历史数据的需求；3、出入库记录：字段是否有值的判断逻辑变更。 |
| 20220809 | v2.0 | 黄艺平 | 增加：1、出入库记录：产品未开启批次/保质期，在出入库记录中不记录单据中填写的批次保质期相关信息（导入）2、功能上线时，将上述多余的信息清除。 |
| 20220810 | v2.1 | 黄艺平 | 增加：1、出入库记录：产品开启了批次/保质期，增加插入期初对应维度的期初余额数据；2、功能上线时，增加历史数据处理。 |
| 20220823 | v2.2 | 黄艺平 | 修改：其他出库/销售出库：参考成本的取值逻辑优化（若开启了批次/保质期，一定要对应的批次保质期信息都填好了之后，才能取值，入参不完整都取不到值，返回为null） |

## 1、需求背景

部分企业，需要按照购买/生产商品的成本价，来进行销售定价/核算利润，故需要按照批次来精准定位扣减成本。

## 2、业务流程/架构

一、历史逻辑见（核心看出入库记录、迭代：移动加权平均、设置-期初存货、设置-成本重算）：[<u>http://smbding.superboss.cc/%E6%9C%89%E6%88%90CRM%EF%BC%88%E9%BB%84%E8%89%BA%E5%B9%B3%EF%BC%89/%E8%BF%9B%E9%94%80%E5%AD%98%EF%BC%88%E5%85%A8%EF%BC%89/#p=</u>](http://smbding.superboss.cc/%E6%9C%89%E6%88%90CRM%EF%BC%88%E9%BB%84%E8%89%BA%E5%B9%B3%EF%BC%89/%E8%BF%9B%E9%94%80%E5%AD%98%EF%BC%88%E5%85%A8%EF%BC%89/#p=)概述

二、批次保质期功能见：

[<u>https://tb.raycloud.com/task/61d53729ddbc4e004aafb57e</u>](https://tb.raycloud.com/task/61d53729ddbc4e004aafb57e)

三、成本重算需求见（动态里面有详细的开发需求附件）：

[<u>https://tb.raycloud.com/task/617ba3b574c0072a0c409afc</u>](https://tb.raycloud.com/task/617ba3b574c0072a0c409afc)

本期改为：



![Picture 1](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbYQj1jMOm9/img/125ef3bb-4254-45a3-866f-b67d3287594f.png?Expires=1774937454&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=yBH2vOJ5nnPO%2BRi1dYO37zCtdtw%3D "")

## 3、功能清单

| **模块** | **二级模块** | **功能点** |
|----------|----------------|-------------|
| 设置 | 期初存货 | 1、支持设置是否按批次扣减成本；2、确认后，执行成本重新计算； |
| 库存管理 | 库存查询 | 增加字段记录该产品在对应仓库的成本信息 |
|  | 批次保质期库存 | 增加字段记录该产品在对应仓库批次/保质期成本信息 |
| 业务单据 | 销售出库单/其他出库 | 增加字段标记产品对应的成本信息 |
|  | 所有出入库单据 | 增加新增/删除/重新编辑的校验 |

## 4、需求详情

### 4.1期初存货



![Picture 2](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbYQj1jMOm9/img/8c9574b1-3959-4f9c-88ea-af036fe652f5.png?Expires=1774937454&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=rTWwHpLf0c73UI4sQ8JKMQp6vto%3D "")

| **页面** | **需求** | **备注** |
|----------|----------|----------|
| 变更核算方式 | 增加字段 | 1、新增字段‘按批次/保质期核算成本’，单选，枚举值：是/否，默认值为“否”2、历史记录企业是否完成期初存货初始化的表中，针对已完成初始化的企业，此字段值统一刷成“否”。 |
|  | 页面红字说明内容变更 | 变更后内容：注意：若系统已完成存货核算初始化1）此时若变更上述设置，则系统会按新的核算规则，对历史已核算好的单据出库成本进行重新核算，请谨慎操作！2）每天限制操作5次变更！ |
|  | 操作（确认） | 企业已完成初始化，变更前后，\{计价方式\}or \{按批次/保质期核算成本\} 值不同：一、若\{按批次/保质期核算成本\}由‘否’改为‘是’1\. 若出入库记录中存在明细行\{批次保质期结存数量\}\<0时，则变更失败并toast提示‘存在开启了批次/保质期的产品明细，出入库记录中'批次保质期结存数量'\<0,无法开启按批次/保质期核算成本，请将对应单据的业务日期进行调整后再进行尝试。’2\. 上述校验通过，则变更成功1. 执行成本重算任务，并在设置-成本重算列表中记录任务情况；2. 按新的核算方式，更新出入库记录中对应明细行的数量、成本相关信息；（计价方式为先进先出，\{批次保质期结存成本\}\{批次保质期单位成本\}字段也有值）二、若\{按批次/保质期核算成本\}由‘是’改为‘否’1\. 执行成本重算任务，并在设置-成本重算列表中记录任务情况2\. 按新的核算方式，更新出入库记录中对应明细行的数量、成本相关信息，并清空\{批次保质期结存成本\}\{批次保质期单位成本\}信息；三、其他信息变更情况，确认后按新的核算规则对系统的成本进行重新核算。 |
| ~~期初存货~~ | ~~变更核算方式操作增加校验~~ | ~~企业存在正在执行中的批次保质期库存预校验任务时，在点击‘变更核算方式’按钮时，toast提示‘正在执行变更存货核算方式的库存预校验任务，暂无无法操作，请稍后重试’。~~ |
| 完成初始化 | 变更弹窗说明 | 当核算方式中，\{按批次/保质期核算成本\}=‘是’，在完成初始化确认的弹窗中，内容变更为‘确认后将按照【\{存货核算计价方式\}-按批次/保质期核算成】的方式计算出库成本，是否确认完成初始化？’ |

### 4.2操作日志

| **需求** | **备注** |
|----------|----------|
| 变更核算方式的日志中标记变更后的存货核算规则 | 日志的操作内容变更：改之前：\[期初存货\]改之后：\[期初存货\]\{变更后存货核算计价方式\}-按批次/保质期核算成本（\{变更后按批次/保质期核算成本\}）举例：\[期初存货\]先进先出-按批次/保质期核算成本（是） |

### 4.3出入库记录

备注：此处只补充产品明细启用了批次/保质期\+须按批次/保质期扣减成本的逻辑

| **需求** | **备注** |
|----------|----------|
| 单位成本 | 一、核算方式为“先进先出”：保持目前逻辑二、核算方式为“移动加权平均”1）期初余额、期初库存、采购入库、销退入库、其他入库、调拨入库、盘点单（盘盈明细行）、组装单（成品）、调拨单（调入仓库）、采购退货出库：保持目前逻辑；2）其他出库、销售出库、调拨出库、盘点单（盘亏明细行）、组装单（原材料）、调拨单（调出仓库）：按照产品明细\+仓库\+批次/保质期维度a.上行结存单价≥0：本次出库时取最近一行（-\{批次保质期单位成本\}）；b.上行结存单价\<0：本次出库时单位成本为0 |
| 成本 | 一、核算方式为“先进先出”1\. 期初余额、期初库存、采购入库、销退入库、其他入库、调拨入库、盘点单（盘盈明细行）、组装单（成品）、调拨单（调入仓库）、采购退货出库：保持目前逻辑；2\. 其他出库、销售出库、调拨出库、盘点单（盘亏明细行）、组装单（原材料）、调拨单（调出仓库）：基础核算规则保持目前逻辑，不过核算的维度要按照‘产品明细\+仓库\+批次/保质期’备注（后同，不再赘述）：1. 只开启批次，就按产品明细\+仓库\+批次2. 只开启保质期：就按产品明细\+仓库\+生产日期\+保质期天数3. 开启批次\+保质期：就按产品明细\+仓库\+批次\+生产日期\+保质期天数二、核算方式为“移动加权平均”：保持目前逻辑 |
| 结存成本 | 保持目前逻辑 |
| 结存单价 | 保持目前逻辑 |
| 单行结存成本 | 一、核算方式为“先进先出”基础核算规则保持目前逻辑，不过核算的维度要按照‘产品明细\+仓库\+批次/保质期’二、核算方式为“移动加权平均”：字段值为空 |
| 单行结存单价 | 一、核算方式为“先进先出”：保持目前逻辑二、核算方式为“移动加权平均”：字段值为空 |
| 已出库数量 | 一、核算方式为“先进先出”基础核算规则保持目前逻辑，不过核算的维度要按照‘产品明细\+仓库\+批次/保质期’二、核算方式为“移动加权平均”：字段值为空 |
| 可出库数量 | 一、核算方式为“先进先出”：保持目前逻辑二、核算方式为“移动加权平均”：字段值为空 |
| 批次保质期结存数量 | 一、新增该字段：系统字段、数值、默认不必填、默认不隐藏、不可更改字段名、新建编辑页不可见（出入库记录列表页可见）、不可设置默认值、排在字段‘失效日期’之后二、计算规则1\. 计算逻辑为按照‘产品明细\+仓库\+批次/保质期’维度：上一条数据\{批次保质期结存数量\}\+该行数据\{库存变化量\}（无上一条数据则计上一条数据\{批次保质期结存数量\}为0）2\. 其余情况：字段值为0 |
| 批次保质期结存成本 | 一、新增该字段：系统字段、数值、默认不必填、默认不隐藏、不可更改字段名、新建编辑页不可见（出入库记录列表页可见）、不可设置默认值、排在字段‘批次保质期结存数量’之后二、计算规则1\. 期初已完成初始化 and  \{按批次/保质期核算成本\}=‘是’ \+ 字段有值\+ 计算逻辑为按照‘产品明细\+仓库\+批次/保质期’维度：上一条数据\{批次保质期结存成本\}\+该行数据\{成本\}（上一条数据\{批次保质期结存成本\}不存在时计上一条为0）2\. 其余情况：字段值为null |
| 批次保质期单位成本 | 一、新增该字段：系统字段、数值、默认不必填、默认不隐藏、不可更改字段名、新建编辑页不可见（出入库记录列表页可见）、不可设置默认值、排在字段‘批次保质期结存成本’之后二、计算规则1\. 期初已完成初始化 and \{按批次/保质期核算成本\}=‘是’ \+ 字段有值\+ 计算逻辑为按照‘产品明细\+仓库\+批次/保质期’维度：本行\{批次保质期结存成本\}/\{批次保质期结存数量\}（上一条数据\{批次保质期结存成本\}不存在时计上一条为0）2\. 其余情况：字段值为null |

### 其他需求：

| **需求** | **备注** |
|----------|----------|
| 历史数据处理 | 一、功能上线时，按照\{批次保质期结存数量\}的计算逻辑，对历史出入库记录数据进行计算（可以为负）二、功能上线时，若：1）产品明细\{开启批次\}='否'，清除掉出入库记录中对应‘批次’字段值；2）产品明细\{开启保质期\}='否'，清除掉出入库记录中对应‘生产日期’‘保质期天数’‘失效日期’字段值；三、功能上线时，若企业期初存货已完成初始化按照企业批次保质期库存表中的情况，增加插入出入库记录/期初存货表中期初余额数据原则为：对应产品明细\+仓库\+批次/保质期维度已存在期初余额数据，则不用增加插入，若没有的，则按照系统自动生成期初余额数据的逻辑进行插入（插入的数据操作时间取历史已有的期初余额的\{操作时间\}，举例： |
| 单据导入生成出入库记录优化 | 现有的模块导入（采购入库单/其他入库单/组装单）生成出入库记录时：1、产品明细\{启用批次\}=‘是’，在出入库记录中记录明细行批次信息；2、产品明细\{启用保质期\}=‘是’，在出入库记录中记录明细行生产日期、保质期天数、失效日期信息；除以上情况，单据中填写的信息均不同步到出入库记录 |
| 期初余额数据生成优化 | 若企业已完成初始化，且产品明细启用了批次or 保质期，当对应产品明细\+仓库\+批次/保质期维度第一次生成出入库记录时，需要自动生成一条对应维度的期初余额数据，举例 |

![Picture 3](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbYQj1jMOm9/img/21f7ff4a-ec0d-4d5c-8486-172ccfd76352.png?Expires=1774937454&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=ZWo2PigRAVLj5VEEvossS1tvRX8%3D "")

![Picture 4](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbYQj1jMOm9/img/ec00b861-5eb3-40bf-9e6d-4e73216abe5e.png?Expires=1774937454&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=oIaAlvHgQYEFY9D3op2ZQH6wAzw%3D "")

### 4.4库存查询

字段需求：

| **字段名称** | **变更类型** | **字段属性** | **字段类型** | **必填属性** | **显隐属性** | **是否可更改字段名** | **新建编辑页可见** | **是否可设默认值** | **字段说明** | **备注** |
|----------------|----------------|----------------|----------------|----------------|----------------|----------------------------|-------------------------|-------------------------|----------------|----------|
| 单位成本 | 新增字段 | 系统字段 | 数值 | 默认不必填 | 默认不隐藏 | 不可更改字段名 | 否 | 否 | 无 | 取出入库记录中对应产品明细\+仓库最新一条明细\{结存单价\}备注：成本重算任务执行完后增加同步到此 |
| 成本 | 新增字段 | 系统字段 | 数值 | 默认不必填 | 默认不隐藏 | 不可更改字段名 | 否 | 否 | 无 | 取出入库记录中对应产品明细\+仓库最新一条明细\{结存成本\}备注：成本重算任务执行完后增加同步到此 |

其他需求：

| **需求** | **备注** |
|----------|----------|
| 移动端列表展示字段优化 | 1、增加展示字段‘产品类型’‘规格型号’，放在‘产品明细名称’之后；2、增加展示字段‘单位成本’‘成本’，放在最后； |

### 4.5批次保质期库存

字段需求：

| **字段名称** | **变更类型** | **字段属性** | **字段类型** | **必填属性** | **显隐属性** | **是否可更改字段名** | **新建编辑页可见** | **是否可设默认值** | **字段说明** | **备注** |
|----------------|----------------|----------------|----------------|----------------|----------------|----------------------------|-------------------------|-------------------------|----------------|----------|
| 单位成本 | 新增字段 | 系统字段 | 数值 | 默认不必填 | 默认不隐藏 | 不可更改字段名 | 否 | 否 | 无 | 取出入库记录中对应产品明细\+仓库\+批次/保质期最新一条明细\{批次保质期单位成本\} |
| 成本 | 新增字段 | 系统字段 | 数值 | 默认不必填 | 默认不隐藏 | 不可更改字段名 | 否 | 否 | 无 | 取出入库记录中对应产品明细\+仓库最新一条明细\{批次保质期结存成本\} |

其他需求：

| **需求** | **备注** |
|----------|----------|
| 移动端列表展示字段优化 | 1、增加展示字段‘产品类型’‘规格型号’，放在‘产品明细名称’之后；2、增加展示字段‘单位成本’‘成本’，放在最后； |

### 4.6其他出库单/销售出库单产品

| **字段名称** | **变更类型** | **字段属性** | **字段类型** | **必填属性** | **显隐属性** | **是否可更改字段名** | **新建编辑页可见** | **是否可设默认值** | **字段说明** | **备注** |
|----------------|----------------|----------------|----------------|----------------|----------------|----------------------------|-------------------------|-------------------------|----------------|----------|
| 参考成本 | 新增字段 | 系统字段 | 数值 | 默认不必填 | 默认不隐藏 | 可更改 | 是 | 否 | 为新建单据时获取到的单位成本，仅供参考 | 1、新建编辑页均不可编辑（置灰）2、字段排序：默认排在字段‘单价’之前。 |

| **需求** | **备注** |
|----------|----------|
| 参考成本字段处理 | 一、字段信息须存入数据库（不是参考字段）二、取数规则1\. \{按批次/保质期核算成本\}=‘否’ or （产品明细未启用批次 and 保质期)则：根据明细行\{仓库\}\+\{产品明细\}，查询库存表的\{单位成本\}1. 字段‘仓库’为空或者库存表里面未查询到数据，\{单位成本\}为空（为null，不是0）；2. 变更明细行\{仓库\}，需要重新查询\{单位成本\}；2\. \{按批次/保质期核算成本\}=‘是’and (产品明细启用了批次 or 保质期)1. 若只开启了批次：根据明细行\{仓库\}\+\{产品明细\}\+\{批次\}查询批次保质期库存表的\{单位成本\}1. 参数不完整或者表中未查询到数据，\{单位成本\}为空（为null，不是0）；2. 变更批次 or 仓库，则更新\{单位成本\}2. 若只开启了保质期：根据明细行\{仓库\}\+\{产品明细\}\+\{生产日期\}\+\{保质期天数\}查询批次保质期库存表的\{单位成本\}1. 参数不完整或者表中未查询到数据，\{单位成本\}为空（为null，不是0）；2. 变更生产日期or 保质期天数 or 仓库，则更新\{单位成本\}3. 若开启了批次保质期：根据明细行\{仓库\}\+\{产品明细\}\+\{批次\}\+\{生产日期\}\+\{保质期天数\}查询批次保质期库存表的\{单位成本\}1. 参数不完整或者表中未查询到数据，\{单位成本\}为空（为null，不是0）；2. 变更批次 or 生产日期or 保质期天数 or 仓库，则更新\{单位成本\} |

### 4.6其他单据增加批次保质期库存预校验

原因：



![Picture 5](https://alidocs2.oss-cn-zhangjiakou.aliyuncs.com/res/Lk3lbmbYQj1jMOm9/img/93444559-1276-48c0-8694-ccb6ac0e87b7.png?Expires=1774937454&OSSAccessKeyId=LTAI5tKTjg4Kq1HCdBJ8qpSp&Signature=F2uYIPYgd2QLmZEnFTPk%2Bea4FJU%3D "")

解决方案：

| **需求** | **备注** |
|----------|----------|
| 总校验规则 | 校验前提：企业已完成存货核算初始化 and \{按批次/保质期核算成本\}=‘是’ and （产品明细\{启用批次\}=‘是’ or \{启用保质期\}=‘是’）预校验逻辑：出入库记录中\{批次保质期结存数量\}不能\<0 |
| 涉及单据及处理 | 一、须增加校验的单据及操作1\. 其他入库单/采购入库单/销退入库单/调拨入库单\+ 单据删除\+ 单据提交（重新编辑提交）：提交审批后无法立即通过（逻辑同单据删除）\+ 单据提交（重新编辑提交）：提交审批后立即通过2\. 其他出库单/销售出库单/采退出库单/调拨出库单\+ 单据提交（新增单据的审批通过）\+ 单据提交（重新编辑提交）：提交审批后立即通过3\. 组装单/盘底单/调拨单（仅针对\{调拨类型\}=‘直接调拨’）\+ 单据删除\+ 单据提交（新增单据的审批通过）\+ 单据提交（重新编辑提交）：提交审批后无法立即通过（逻辑同单据删除）\+ 单据提交（重新编辑提交）：提交审批后立即通过二、校验不通过的提示信息\+ 单据删除：toast提示“单据删除会导致\{产品明细名称\}（\{系统编码\}）成本扣减可出库数量不足（批次保质期库存），提交失败”（目前无批量删除）\+ 单据提交（新增单据的审批通过）：oast提示“\{产品明细名称\}（\{系统编码\}）成本扣减可出库数量不足（批次保质期库存），提交失败”\+ 单据提交（重新编辑提交）：toast提示“修改后会导致\{产品明细名称\}（\{系统编码\}）成本扣减可出库数量不足（批次保质期库存），提交失败” |
